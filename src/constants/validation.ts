import { ValidationMessagesTypes } from 'types/constants/constantsTypes';

const VALIDATION_MESSAGES: ValidationMessagesTypes = {
  INVALID_INPUT: 'Invalid input',
  REQUIRED_FIELD: 'This field is required'
};

export default VALIDATION_MESSAGES;
